import UIKit

struct ShapeData {
    let fillColor: UIColor
    let gradientColors: [CGColor]
    let shadowColor: UIColor
    let shadowOffset: CGSize
    var backgroundImage: UIImage? // Сюда записывается фото при срабатывании жеста
}
